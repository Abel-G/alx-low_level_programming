#include "main.h"
/**
* _isupper - Entry point
* @c: integer received
* Description: checks uppercase chx
* Return: 1 if upper case 0 otherwise
*/
int _isupper(int c)
{
if (c >= 65 && c <= 97)
{
return (1);
}
else
{
return (0);
}
}
